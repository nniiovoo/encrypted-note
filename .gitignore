/target
node_modules/
/dist
/dist-isolation
.DS_Store
*.enote
*.tmp-*
# local vault data must never be committed
/src-tauri/target

# local AI-tooling databases (Ruflo), not project files
ruvector.db
agentdb.rvf
agentdb.rvf.lock

# regenerated by tauri-build
/src-tauri/gen/
/src-tauri/permissions/autogenerated/

# planning docs (PRD, task files) stay on this computer
.scratch/

# macOS metadata sidecars on exFAT/FAT drives (e.g. a portable disk)
._*
