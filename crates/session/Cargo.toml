[package]
name = "session"
version = "0.1.0"
description = "Locked/Unlocked state machine, Auto-lock, wrong-password delays, reveal timers, clipboard timing, backup reminder rule. Pure; clock passed in."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]

[lints]
workspace = true
